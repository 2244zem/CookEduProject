# Local dev: Vite proxies /api → http://localhost:8000
VITE_API_URL=/api

# Cloudflare production: keep /api (same-origin proxy in functions/api/[[path]].js)
# Do NOT set direct Railway URL unless you also fix CORS manually:
# VITE_API_URL=https://cookeduproject-production.up.railway.app/api
