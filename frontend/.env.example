# Local dev: Vite proxies /api to Laravel (see vite.config.ts)
# VITE_API_URL=/api

# Production (Cloudflare build): full Railway API base URL
VITE_API_URL=https://cookeduproject-production.up.railway.app/api
